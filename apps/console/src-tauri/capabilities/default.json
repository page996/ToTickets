{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "console-main",
  "description": "Main window limited to the runtime configuration application command",
  "windows": ["main"],
  "permissions": ["allow-get-console-runtime-config"]
}
