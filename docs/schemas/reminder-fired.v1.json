{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/schemas/reminder-fired.v1.json",
  "title": "ReminderFiredV1",
  "type": "object",
  "additionalProperties": false,
  "required": ["sequence", "schedule_id", "channel", "state"],
  "properties": {
    "sequence": {"type": "integer", "minimum": 1},
    "schedule_id": {"type": "string", "format": "uuid"},
    "channel": {"enum": ["desktop", "sound"]},
    "state": {"const": "notified"}
  }
}
