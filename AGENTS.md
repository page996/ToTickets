# 项目代理约束

本文件是本仓库的项目级开发约束。它不能修改 Codex 的隐藏 system prompt，也不要求展示或复述内部思维链；它只约束本仓库内的代码、文档和工具操作。

## 项目定位

本项目是“人工购票辅助控制台”的研究和工程实现。它可以管理用户自己的 Android 设备/模拟器、提供只读画面观察、健康状态、倒计时提醒、人工确认和审计能力。

真实大麦 App 的购票动作必须由人完成。以下能力永远不在生产适配器中实现，也不得通过换名、脚本、私有接口或插件间接实现：

- 自动登录、读取/提交短信验证码、自动完成验证码或风控挑战
- OCR/视觉识别后自动点击、坐标宏、循环点击、批量点击或跨设备广播输入
- 自动选择票档、填写实名信息、提交订单、锁票、支付或输入支付密码
- 逆向或调用大麦未公开接口、重放令牌、绕过设备指纹、频率限制或封禁
- 通过多账号同步提高购票概率，或以任何方式规避平台规则

本地 mock App 可以使用自动化测试来验证调度、状态机和接口，但 mock 行为不得迁移到真实大麦适配器。

## 操作审批与沟通

- 任何文件写入、依赖安装、外部网络访问、设备启动、进程启动或配置变更，都要先在对话中说明原因、范围、预期结果和风险；没有明确批准时停止在只读检查。
- 用户已经给出持续性批准时，仍要在每个有实质影响的阶段发送简短进度说明，并在结果改变原方案时说明实际差异。
- 只读检查优先；不得覆盖或删除用户已有文件。破坏性操作必须先解析目标并获得单独批准。
- 不得修改隐藏 system prompt。需要持久化约束时，更新本文件或 `docs/governance/`，并说明其作用域。

## 代码与架构规则

- 技术栈已冻结：后端只能使用 NestJS/TypeScript；控制台只能使用 Tauri + React/TypeScript。不得引入 FastAPI、Python 后端、Electron 或第二套服务端框架作为实现；替代方案只能出现在调研文档中。
- 不得在源码、测试或打包脚本中硬编码主机地址、端口、文件系统绝对路径、用户目录、临时目录、ADB/模拟器可执行文件路径或凭据。运行时值必须来自版本化配置 schema、环境变量或用户选择的项目相对路径，并在启动时校验。
- 文档中的外部来源链接、协议路由（例如 `/api/v1`）和 schema 标识不属于运行时地址；它们必须明确标注为文档/协议常量，不能被复用为部署默认值。
- 不使用系统级 Node、Rust 或其他运行时作为项目假设。开发和构建必须使用项目声明的隔离工具链（版本文件、包管理器锁文件和 Rust toolchain 文件）；不得把全局安装状态当作依赖。
- 每个直接和间接依赖都必须在依赖清单、锁文件和许可证/SBOM 记录中可追溯；新增依赖先说明用途、版本、来源、许可证、权限面和替代方案。部署文档必须支持在新机器用锁文件复现，或生成自包含安装包。
- 模块通过版本化 REST/WebSocket 合约连接，不共享数据库表结构或内部类。
- 所有外部设备能力通过 `DeviceAdapter` 抽象；默认实现是观察和生命周期管理，输入能力默认关闭。
- 真实平台适配器只能报告状态、展示画面和打开由用户明确请求的应用；不能暴露购买流程自动化接口。
- 默认本地部署、最小权限、无云端上传。敏感信息不进入控制台数据库、日志、录屏或错误追踪。
- 时间统一使用 UTC 存储和单调时钟计算；所有提醒都必须可审计、可取消、可重复验证。
- API 使用 OpenAPI 兼容的 JSON；事件使用 CloudEvents 风格的 envelope，并包含 schema 版本。
- 依赖必须有许可证、维护状态和用途记录；未经评估的闭源模拟器只允许在人工观察试验中使用。
- 所有外部地址和本地路径由 `@nestjs/config`（或等价的严格配置层）集中加载；配置中禁止秘密默认值，示例只能使用占位符。配置变更必须有 schema 版本和审计记录。

## 测试规则

- 每个模块先有单元测试和契约测试，再接入设备。
- 真实大麦只做人工验收；自动化测试只能针对仓库内 mock App 或脱机录制的非敏感样本。
- 测试不得使用真实账号、手机号、身份证、验证码、支付凭据或真实订单。
- 任何涉及设备输入、批量调度、权限、录屏或数据保留的改动，都必须增加负向测试和审计断言。

## 文档规则

- 需求、接口、威胁模型和验收标准先于实现；变更需要更新对应文档和 ADR。
- 结论必须区分“已由来源确认”“工程假设”“待用户/平台确认”。
- 不引用或整理规避平台风控的攻略；如发现相关材料，只记录风险类别，不记录可执行步骤。

## 环境与交付规则

- 前端/后端统一使用 pnpm workspace；Node 版本、pnpm 版本和 Rust 版本必须固定并提交版本文件，CI 使用同一版本。
- Tauri 的打包工具链、Android SDK/ADB 和 scrcpy 只通过可配置的工具发现器接入；发现失败时给出安装说明，不猜测系统路径。
- 提交前运行依赖审计、许可证检查、SBOM 生成、配置 schema 校验和可复现构建检查。
- 不能使用 `pip install` 作为本项目部署步骤，因为正式后端不使用 Python；若未来出现文档生成等辅助脚本，必须放入独立隔离环境并单独记录，不能成为运行时依赖。
